
<resources xmlns:tools="http://schemas.android.com/tools">
<!-- Base application theme. -->
<style name="Base.Theme.SneakerCity" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Status bar transparente -->
    <item name="android:statusBarColor">@android:color/transparent</item>

    <!-- Permite dibujar detrás del sistema -->
    <item name="android:windowDrawsSystemBarBackgrounds">true</item>

    <!-- Para que los iconos sean oscuros (si fondo es claro) -->
    <item name="android:windowLightStatusBar">true</item>
    <!-- Customize your light theme here. -->
    <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
</style>


<style name="Theme.SneakerCity" parent="Base.Theme.SneakerCity" />
</resources>